{
  "name": "my-blog",
  "version": "2.0.0",
  "author": "Jason Quach <thuanqh@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@theme-ui/color": "^0.3.5",
    "@theme-ui/presets": "^0.3.5",
    "@theme-ui/prism": "^0.3.5",
    "gatsby": "^2.31.1",
    "gatsby-plugin-theme-ui": "^0.3.5",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "theme-ui": "^0.3.5"
  },
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "clean": "gatsby clean && rm -rf node_modules && rm yarn.lock",
    "build": "gatsby build",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public -b master",
    "dev": "gatsby develop"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
